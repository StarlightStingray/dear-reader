//this file is generated by program(drogon_ctl) automatically,don't modify it!
#include "Create.h"
#include <drogon/utils/OStringStream.h>
#include <string>
#include <map>
#include <vector>
#include <set>
#include <iostream>
#include <unordered_map>
#include <unordered_set>
#include <algorithm>
#include <list>
#include <deque>
#include <queue>
using namespace drogon;
std::string Create::genText(const DrTemplateData& Create_view_data)
{
	drogon::OStringStream Create_tmp_stream;
	std::string layoutName{""};
	Create_tmp_stream << "<!DOCTYPE html>\n";
	Create_tmp_stream << "<html>\n";
    auto para=Create_view_data.get<std::unordered_map<std::string,std::string>>("parameters");
	Create_tmp_stream << "<head>\n";
	Create_tmp_stream << "    <meta charset=\"UTF-8\">\n";
	Create_tmp_stream << "    <link rel=\"stylesheet\" href=\"https://cdn.jsdelivr.net/npm/water.css@2/out/water.css\">\n";
	Create_tmp_stream << "    <link rel=\"stylesheet\" href=\"/index.css\" />\n";
	Create_tmp_stream << "    <script defer src=\"/script.js\"></script>\n";
	Create_tmp_stream << "    <title>";
{
    auto & val=Create_view_data["title"];
    if(val.type()==typeid(const char *)){
        Create_tmp_stream<<*any_cast<const char *>(&val);
    }else if(val.type()==typeid(std::string)||val.type()==typeid(const std::string)){
        Create_tmp_stream<<*any_cast<const std::string>(&val);
    }
}
	Create_tmp_stream << "</title>\n";
	Create_tmp_stream << "</head>\n";
	Create_tmp_stream << "<body>\n";
	Create_tmp_stream << "    <h1><a href=\"/\">";
{
    auto & val=Create_view_data["title"];
    if(val.type()==typeid(const char *)){
        Create_tmp_stream<<*any_cast<const char *>(&val);
    }else if(val.type()==typeid(std::string)||val.type()==typeid(const std::string)){
        Create_tmp_stream<<*any_cast<const std::string>(&val);
    }
}
	Create_tmp_stream << "</a></h1>\n";
Create_tmp_stream<<"\n";
	Create_tmp_stream << "    <form action=\"/addStory\" method=\"post\">\n";
	Create_tmp_stream << "        <label for=\"storytitle\">Title of story:</label><br>\n";
	Create_tmp_stream << "        <textarea rows=\"20\" cols=\"120\" id=\"TITLE\">\n";
	Create_tmp_stream << "        </textarea>\n";
	Create_tmp_stream << "        <div id=\"chapters\">\n";
	Create_tmp_stream << "        <a href=\"#\" id=\"addChapter\" onClick=\"addFields()\">Add chapter</a>\n";
	Create_tmp_stream << "        <input type=\"submit\" value=\"Submit\" id=\"\">\n";
	Create_tmp_stream << "    </form> \n";
	Create_tmp_stream << "</body>\n";
	Create_tmp_stream << "</html>\n";
if(layoutName.empty())
{
std::string ret{std::move(Create_tmp_stream.str())};
return ret;
}else
{
auto templ = DrTemplateBase::newTemplate(layoutName);
if(!templ) return "";
HttpViewData data = Create_view_data;
auto str = std::move(Create_tmp_stream.str());
if(!str.empty() && str[str.length()-1] == '\n') str.resize(str.length()-1);
data[""] = std::move(str);
return templ->genText(data);
}
}
